# service typu clusterip - dostepny tylko wewnatrz klastra
# backend laczy sie z baza przez nazwe postgres-service
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
